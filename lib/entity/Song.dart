class Song {
  Song(this.id, this.name, this.songer, this.duration, this.playId,
      this.isDownload);
  int id;
  String name;
  String songer;
  String duration;
  int playId;
  bool isDownload;
}
